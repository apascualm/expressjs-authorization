const AuthorizationMiddleware = require('./authorization.middleware');
const AuthorizationExpressMiddleware = require('./authorizationExpress.middleware');

module.exports = {
  AuthorizationMiddleware,
  AuthorizationExpressMiddleware,
};
